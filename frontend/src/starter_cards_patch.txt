PATCH: Add Starter Cards to Simple Mode

1. Find where Simple Mode is rendered (uiMode === "simple").

2. Insert Starter Cards block above the main content.

3. Ensure function exists:
executeCommandFlow(cmd) -> calls your existing command handler.

4. Cards:
- Build Dashboard -> "make dashboard"
- Dev Workspace -> "make dev workspace"
- SaaS Landing -> "make saas landing"

This patch avoids modifying your existing logic.
